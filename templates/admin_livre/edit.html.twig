{% extends 'base-admin.html.twig' %}

{% block title %}Modifier un livre{% endblock %}

{% block body %}
    <h1>Modifier Livre</h1>

    {{ include('admin_livre/_form.html.twig', {'button_label': 'Confirmer'}) }}

    <div class="d-flex justify-content-evenly align-items-center">
    <a href="{{ path('admin_livre_index') }}" button type="button" class="btn btn-dark">Retour à la liste</a>

    {{ include('admin_livre/_delete_form.html.twig') }}
    </div>
{% endblock %}
