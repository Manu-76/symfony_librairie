<form method="post" action="{{ path('admin_livre_delete', {'id': livre.id}) }}"  onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre.id) }}">
<button class="btn btn-danger my-3"><i class="fa fa-trash" aria-hidden="true"></i></button>
</form>
